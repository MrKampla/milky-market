{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["$POLYGON_MAINNET_RPC", "$PRIVATE_KEY", "$PRIVATE_KEY_2"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
